services:
  - type: web
    name: niche-connect-api
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node src/backend/server.js
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
