services:
  - type: web
    name: niche-connect-api
    env: node
    buildCommand: cd api/backend && npm install
    startCommand: cd api/backend && node server.js
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
