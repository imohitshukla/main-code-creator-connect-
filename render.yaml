services:
  - type: web
    name: niche-connect-api
    env: node
    buildCommand: npm install
    startCommand: node api/server.js
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production
